Counting Sheep:
    This will callback N times so the Big O notation is O(n).

Factorial:
    This will callback N times so the Big O notation is O(n).

Fibonacci: 
    This will callback N times so the Big O notation is O(n).

Power Calculator:
    This will callback N times so the Big O notation is O(n).

Reverse_string:
    This will callback N times so the big O notation is O(n).

Triangular Number:
    This will callback N times so the Big O notation is O(n).

String Splitter:
    This will callback N times so the Big O notation is O(n).
